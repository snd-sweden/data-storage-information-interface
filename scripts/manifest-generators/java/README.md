# Java command line example to generate RO-Crate manifest

Example of how to generate RO-Crate manifest from a local file directory in Java.

# Build
`mvn compile`

# Run
`mvn exec:java -Dexec.args="/path/to/dataset"`